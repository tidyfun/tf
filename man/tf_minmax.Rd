% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/depth.R
\name{tf_minmax}
\alias{tf_minmax}
\alias{min.tf}
\alias{max.tf}
\alias{range.tf}
\title{Depth-based minimum, maximum and range for \code{tf} vectors}
\usage{
\method{min}{tf}(..., na.rm = FALSE, depth = NULL)

\method{max}{tf}(..., na.rm = FALSE, depth = NULL)

\method{range}{tf}(..., na.rm = FALSE, depth = NULL)
}
\arguments{
\item{...}{\code{tf} objects (and \code{na.rm} for the pointwise default).}

\item{na.rm}{logical; passed on to the pointwise summary or used to filter
\code{NA}s before computing depth.}

\item{depth}{depth method to use. \code{NULL} (default) gives the pointwise
min/max/range. Supply a depth name (e.g. \code{"MHI"}) or a custom depth
function for depth-based selection.}
}
\value{
a \code{tf} object.
}
\description{
By default, \code{min}, \code{max}, and \code{range} compute \strong{pointwise} extremes (the
existing behaviour). When a \code{depth} argument is supplied, they instead return
the most extreme / most central observation according to the chosen depth.
For the default \code{"MHI"} depth this gives the lowest / highest function in an
up-down sense.
}
\examples{
x <- tf_rgp(5) + 1:5
# pointwise (default):
min(x)
max(x)
# depth-based:
min(x, depth = "MHI")
max(x, depth = "MHI")
}
\seealso{
\code{\link[=tf_depth]{tf_depth()}}, \code{\link[=rank.tf]{rank.tf()}}

Other tidyfun ordering and ranking functions: 
\code{\link{tf_depth}()},
\code{\link{tf_order}}
}
\concept{tidyfun ordering and ranking functions}
