% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/depth.R
\name{tf_order}
\alias{tf_order}
\alias{rank}
\alias{rank.default}
\alias{rank.tf}
\alias{xtfrm.tf}
\alias{sort.tf}
\alias{tf_order.default}
\alias{tf_order.tf}
\alias{tf_order.tf_mv}
\title{Rank, order and sort \code{tf} vectors}
\usage{
rank(
  x,
  na.last = TRUE,
  ties.method = c("average", "first", "last", "random", "max", "min"),
  ...
)

\method{rank}{default}(
  x,
  na.last = TRUE,
  ties.method = c("average", "first", "last", "random", "max", "min"),
  ...
)

\method{rank}{tf}(
  x,
  na.last = TRUE,
  ties.method = c("average", "first", "last", "random", "max", "min"),
  depth = "MHI",
  ...
)

\method{xtfrm}{tf}(x)

\method{sort}{tf}(x, decreasing = FALSE, na.last = NA, depth = "MHI", ...)

tf_order(f, ...)

\method{tf_order}{default}(f, ...)

\method{tf_order}{tf}(f, depth = "MHI", ...)

\method{tf_order}{tf_mv}(f, by = "norm", ...)
}
\arguments{
\item{x}{a \code{tf} vector.}

\item{na.last}{for handling of \code{NA}s; see \code{\link[base:rank]{base::rank()}} and \code{\link[base:sort]{base::sort()}}.}

\item{ties.method}{a character string for handling ties; see \code{\link[base:rank]{base::rank()}}.}

\item{...}{passed to \code{\link[=tf_depth]{tf_depth()}} (e.g. \code{arg}).}

\item{depth}{the depth function to use for ranking. One of the depths
available via \code{\link[=tf_depth]{tf_depth()}} (default: \code{"MHI"}) or a function that takes a
\code{tf} vector and returns a numeric vector of depth values.}

\item{decreasing}{logical. Should the sort be decreasing?}

\item{f}{a \code{tf} or \code{tf_mv} vector (for \code{tf_order}).}

\item{by}{(\code{tf_mv} only) the scalar reduction to order by: \code{"norm"} (the
default, uses \code{tf_norm(f)}) or the name of a component.}
}
\value{
\code{rank}: a numeric vector of ranks.\cr
\code{tf_order}: an integer vector of indices.\cr
\code{sort.tf}: a sorted \code{tf} vector.\cr
\code{xtfrm.tf}: a numeric vector of depth values.
}
\description{
These methods use \code{\link[=tf_depth]{tf_depth()}} to rank, order, and sort functional data. By
default they use the modified hypograph index (\code{"MHI"}) which provides an
up-down ordering (lowest to highest). You can also use any of the other depth
methods available via \code{\link[=tf_depth]{tf_depth()}}, or supply a custom depth function.
}
\details{
\code{rank} assigns ranks based on depth values: lower depth values get lower
ranks. For \code{"MHI"} this gives an ordering from lowest to highest function.
For centrality-based depths (\code{"MBD"}, \code{"FM"}, \code{"FSD"}, \code{"RPD"}), the most
extreme function gets rank 1 and the most central gets the highest rank.

\code{tf_order} returns the permutation which rearranges \code{x} into ascending
order according to depth. For vector-valued (\code{tf_mv}) data there is no
canonical total order on \eqn{R^d}: \code{tf_order.tf_mv()} requires an explicit
scalar reduction via \code{by} (either \code{"norm"} for \code{tf_norm(f)}, or a component
name), and then applies the univariate depth order to that reduction.

\code{sort.tf} returns the sorted \code{tf} vector.

\code{xtfrm.tf} returns a numeric vector of MHI depth values, enabling
\code{base::order} and \code{base::rank} to work on \code{tf} vectors.
}
\examples{
x <- tf_rgp(5) + 1:5
rank(x)
order(x)
sort(x)
# use a centrality-based depth instead:
rank(x, depth = "MBD")
}
\seealso{
\code{\link[=tf_depth]{tf_depth()}}, \code{\link[=min.tf]{min.tf()}}, \code{\link[=max.tf]{max.tf()}}

Other tidyfun ordering and ranking functions:
\code{\link[=tf_depth]{tf_depth()}},
\code{\link{tf_minmax}}
}
\concept{tidyfun ordering and ranking functions}
