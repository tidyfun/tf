% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/register.R
\name{tf_register_landmark}
\alias{tf_register_landmark}
\alias{tf_landmarks_extrema}
\title{Landmark Registration}
\usage{
tf_register_landmark(.x, .landmarks, .template_landmarks = NULL)

tf_landmarks_extrema(x, which = c("max", "min", "both"))
}
\arguments{
\item{.x}{a \code{tf} vector of functions to register.}

\item{.landmarks}{a numeric matrix of landmark positions with one row per
function and one column per landmark feature. Landmarks must be strictly
increasing within each row.}

\item{.template_landmarks}{optional numeric vector of template landmark
positions to align to. Must have the same length as \code{ncol(.landmarks)}.
If \code{NULL} (default), uses the column-wise means of \code{.landmarks}.}

\item{x}{a \code{tf} vector for \code{tf_landmarks_extrema()}.}

\item{which}{character specifying which extrema to find: \code{"max"} for maxima,
\code{"min"} for minima, or \code{"both"} for both (maxima first, then minima).}
}
\value{
a \code{tfd} vector of warping functions with the same length as \code{.x}.
These can be used with \code{\link[=tf_unwarp]{tf_unwarp()}} to align the functions.

For \code{tf_landmarks_extrema()}: a numeric matrix with one row per
function containing the locations of the specified extrema.
}
\description{
\code{tf_register_landmark()} performs piecewise linear registration by aligning
user-specified landmarks across functions. This is useful when functions have
identifiable features (peaks, valleys, zero-crossings) that should be aligned.

\code{tf_landmarks_extrema()} is a helper function that automatically finds the
locations of extrema (maxima and/or minima) in each function.
}
\details{
Landmark registration creates piecewise linear warping functions that map
each curve's landmark locations to template landmark locations. Between
landmarks, the warping function interpolates linearly, stretching or
compressing the time axis to align the features.

The landmarks matrix must have the same number of rows as functions in \code{.x}.
Each row contains the observed landmark positions for that function, and
each column represents one landmark feature. Landmarks must be strictly
increasing within each row and within the domain of \code{.x}.
}
\examples{
# Create functions with shifted peaks
t <- seq(0, 1, length.out = 101)
shifts <- c(0.4, 0.5, 0.6) # peak locations
x <- tfd(
  t(sapply(shifts, function(s) dnorm(t, mean = s, sd = 0.1))),
  arg = t
)
plot(x, col = 1:3)

# Find peak locations and register
peaks <- tf_landmarks_extrema(x, "max")
warp <- tf_register_landmark(x, peaks)
x_aligned <- tf_unwarp(x, warp)
plot(x_aligned, col = 1:3)

}
\seealso{
\code{\link[=tf_register]{tf_register()}} for elastic registration, \code{\link[=tf_register_affine]{tf_register_affine()}}
for affine registration

Other registration functions: 
\code{\link{tf_register_affine}()}
}
\concept{registration functions}
