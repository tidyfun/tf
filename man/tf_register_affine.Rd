% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/register.R
\name{tf_register_affine}
\alias{tf_register_affine}
\title{Affine Registration}
\usage{
tf_register_affine(
  .x,
  .template = NULL,
  .type = c("shift", "scale", "shift_scale"),
  .anchor = c("start", "end", "center")
)
}
\arguments{
\item{.x}{a \code{tf} vector of functions to register.}

\item{.template}{an optional \code{tf} vector of length 1 to use as the template.
If \code{NULL} (default), the cross-sectional mean of \code{.x} is used.}

\item{.type}{character specifying the type of affine transformation:
\code{"shift"}, \code{"scale"}, or \code{"shift_scale"}.}

\item{.anchor}{character specifying the anchor point for scaling:
\code{"start"}, \code{"end"}, or \code{"center"}. Only used when \code{.type} includes scaling.}
}
\value{
a \code{tfd} vector of warping functions with the same length as \code{.x}.
These can be used with \code{\link[=tf_unwarp]{tf_unwarp()}} to align the functions.
}
\description{
\code{tf_register_affine()} performs registration using affine (linear) warping
functions of the form \eqn{h(t) = a \cdot t + b}, where \eqn{a} controls
scaling (dilation/compression) and \eqn{b} controls shift (translation).
}
\details{
Affine registration is simpler than elastic registration and is appropriate
when the phase variability in the data consists only of shifts and/or
uniform scaling of the time axis.

Three types of affine registration are available:
\itemize{
\item \code{"shift"}: Only horizontal translation (\eqn{a = 1}, optimize \eqn{b}).
The functions are shifted left or right to align with the template.
\item \code{"scale"}: Only scaling (\eqn{b} determined by anchor, optimize \eqn{a}).
The time axis is stretched or compressed around the anchor point.
\item \code{"shift_scale"}: Both shift and scale (optimize both \eqn{a} and \eqn{b}).
Combines translation and scaling for more flexible alignment.
}

The \code{.anchor} parameter determines the fixed point for scaling:
\itemize{
\item \code{"start"}: The domain start is fixed; scaling expands/contracts toward the end.
\item \code{"end"}: The domain end is fixed; scaling expands/contracts toward the start.
\item \code{"center"}: The domain center is fixed; scaling expands/contracts symmetrically.
}

Optimization minimizes the L2 distance between each (affinely warped)
function and the template.
}
\examples{
# Create shifted sinusoids
t <- seq(0, 2 * pi, length.out = 101)
shifts <- seq(-0.5, 0.5, length.out = 5)
x <- tfd(t(sapply(shifts, function(s) sin(t + s))), arg = t)
plot(x, col = 1:5)

# Shift registration
warp <- tf_register_affine(x, .type = "shift")
x_aligned <- tf_unwarp(x, warp)
plot(x_aligned, col = 1:5)

}
\seealso{
\code{\link[=tf_register]{tf_register()}} for elastic registration, \code{\link[=tf_register_landmark]{tf_register_landmark()}}
for landmark-based registration

Other registration functions: 
\code{\link{tf_register_landmark}()}
}
\concept{registration functions}
