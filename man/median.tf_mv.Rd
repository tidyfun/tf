% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ops-mv.R
\name{median.tf_mv}
\alias{median.tf_mv}
\title{Joint depth-median for vector-valued functional data}
\usage{
\method{median}{tf_mv}(x, na.rm = FALSE, depth = "MBD", ...)
}
\arguments{
\item{x}{a \code{tf_mv} vector.}

\item{na.rm}{if \code{FALSE} (default), any \code{NA} observation makes the result
\code{NA}; if \code{TRUE}, \code{NA} observations are dropped first.}

\item{depth}{the joint depth method, see \code{\link[=tf_depth]{tf_depth()}}.}

\item{...}{passed to \code{\link[=tf_depth]{tf_depth()}}.}
}
\value{
a length-1 \code{tf_mv}: the observed curve with maximal joint depth.
}
\description{
The median of a \code{tf_mv} vector is the single \emph{observed} curve with maximal
joint depth (see \code{\link[=tf_depth]{tf_depth()}}): one \code{which.max} index selects the same
observation across every component, so the result is never a "chimera"
stitched together from different curves. Note the deliberate divergence from
\code{\link[=median.tf]{median.tf()}} on ties: the univariate median \emph{averages} tied maximal-depth
curves, but averaging components would break the observed-curve guarantee,
so \code{median.tf_mv} returns the first tied curve (with a message). On tied
data, \code{median(f)$x} and \code{median(f$x)} can therefore differ.
}
\seealso{
Other tidyfun summary functions:
\code{\link{fivenum}()},
\code{\link{tfsummaries}}
}
\concept{tidyfun summary functions}
