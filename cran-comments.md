## R CMD check results

0 errors | 0 warnings | 0 notes

## Changes in this version

Major update (v0.3.4 → v0.4.0): new registration API (`tf_register()` returns
`tf_registration` class), continuous-criterion registration backend, functional
depth measures (FM, FSD, RPD), rank/order/sort methods for tf vectors.

## Reverse dependencies

Checked all reverse dependencies. No issues found.
